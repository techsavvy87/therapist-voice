// next.config.ts
import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* Your other config options here */
  devIndicators: false,  // This hides the circular "N" dev button
};

export default nextConfig;